apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowforge.fullname" . }}
  labels:
    {{- include "flowforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "flowforge.selectorLabels" . | nindent 4 }}
